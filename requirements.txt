# Sprint 1 - dataset inspection and preprocessing.
#
# Only packages that the code in src/ and scripts/ actually imports are listed.
# Versions are pinned to what the pipeline was developed and verified against.
#
# Deliberately NOT included:
#   scikit-learn  - the stratified patient-level split is implemented directly in
#                   src/preprocessing/splits.py, so sklearn is not imported.
#   scikit-image  - the noise/contrast metrics in
#                   src/preprocessing/quality_metrics.py are implemented with
#                   numpy + scipy, so skimage is not imported.
#   Pillow        - no 2-D image files are read or written; volumes are .mha and
#                   preprocessed slices are .npz arrays.
# These are commonly listed in medical-imaging projects but would be unused here.

# --- core numerics and tables ---
numpy==2.1.3
pandas==2.2.3
scipy==1.16.3

# --- medical image I/O: reads the .mha (MetaImage) volumes and handles
#     anatomical reorientation (LPS/PIR -> RAS) ---
SimpleITK==2.4.0

# --- 2-D image operations: resizing with explicit interpolation control
#     (nearest neighbour for masks), median/gaussian/bilateral filtering, CLAHE ---
opencv-python==4.10.0.84

# --- figures for outputs/visualizations/ ---
matplotlib==3.10.6

# --- notebook: nbformat builds notebooks/01_preprocessing.ipynb,
#     jupyterlab runs it ---
nbformat==5.9.2
jupyterlab==4.0.11
