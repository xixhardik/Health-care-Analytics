# Frontend configuration. Copy to frontend/.env.local
#
# Only one setting matters: where the FastAPI backend is listening. It is exposed
# to the browser (NEXT_PUBLIC_ prefix) because the browser calls the API directly
# and fetches slice images from it.

NEXT_PUBLIC_API_URL=http://localhost:8000
